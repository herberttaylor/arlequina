Instalar a arlequina bot.

Primeiro extraia a pasta pro armazenamento interno.
Em seguida, abra seu termux(ou outro terminal)

Digite os seguintes comandos separadamente no termux:

termux-setup-storage
(no comando acima ele ira pedir acesso ao seus arquivos internos, aceite)

pkg install
pkg update
pkg upgrade
pkg install bash
pkg install npm
apt install
apt update
apt upgrade


*Sua instalação inicial foi feita*

Agora para instalar o bot, digite os comandos abaixo separadamente

cd /sdcard
ls 
(no ls ele ira mostrar todos seus arquivos, procure o nome do arquivo alerquina)
Caso esteja no jeito padrão, use o comando abaixo

cd ryan
(se aparecer no such file on directory, procure o nome da pasta arlequina)

(caso de sucesso e encontre o diretorio, digite os comandos abaixo)

bash install.sh

npm start

Depois so conectar whastapp web do seu bot